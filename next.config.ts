/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    productionBrowserSourceMaps: false, // Désactive les sources maps en prod
};

module.exports = nextConfig;
